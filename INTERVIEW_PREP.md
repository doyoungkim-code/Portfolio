# INTERVIEW_PREP — 포트폴리오 항목별 면접 방어 노트

> 포트폴리오에 실린 모든 기술 항목에 대해 ① 개념 ② 우리 프로젝트에서의 구현 ③ 예상 질문과 답 포인트를 정리.
> "내가 했지만 기억이 흐릿한" 항목을 다시 내 것으로 만드는 용도. 소리 내어 설명해보고, 막히는 곳을 다시 읽을 것.

---

## PART 1 · SSABREE TIME

### 1. JWT Access + Refresh 이중 토큰

**개념** — JWT(JSON Web Token)는 서버가 세션을 저장하지 않고, 서명된 토큰 자체에 사용자 정보(클레임)를 담아 인증하는 방식. 토큰이 탈취되면 만료 전까지 막을 수 없으므로, **수명이 짧은 Access 토큰**(API 호출용)과 **수명이 긴 Refresh 토큰**(Access 재발급용)으로 나눈다.

**우리 구현** — Access 30분 + Refresh 1년. 클라이언트는 API 요청마다 `Authorization: Bearer <access>` 헤더를 보내고, 401(만료)을 받으면 Refresh 토큰으로 `/reissue` 류 엔드포인트를 호출해 새 Access를 받는다.

**예상 질문**
- "왜 30분/1년인가?" → Access는 탈취 피해 시간을 줄이기 위해 짧게. Refresh는 커뮤니티 앱 특성상 재로그인 마찰을 줄이려 길게. 대신 Refresh는 재발급 전용이라 노출 빈도가 낮다.
- "Refresh 토큰이 탈취되면?" → 서버 측 저장(DB/Redis) 후 대조하면 무효화 가능. 한 단계 더 가면 RTR(Refresh Token Rotation: 재발급 시 Refresh도 새로 발급하고 이전 것 폐기)로 재사용 탐지.
- "세션 방식 대비 장단점?" → 장점: 서버 무상태·수평 확장 용이, 모바일 앱 친화적. 단점: 즉시 강제 로그아웃이 어려움(만료 전 토큰은 유효).

### 2. SSAFY Mattermost 연동 본인 인증

**개념** — 자체 회원가입만으로는 "SSAFY 교육생인지"를 검증할 수 없다. 교육생 전원이 쓰는 Mattermost 계정으로 로그인 검증을 위임해, 폐쇄형 커뮤니티의 가입 자격을 보증한다.

**우리 구현** — 가입 시 사용자가 입력한 Mattermost 자격으로 Mattermost API에 로그인 시도 → 성공 응답이면 SSAFY 구성원으로 간주하고 우리 서비스 계정을 발급.

**예상 질문**
- "왜 이 방식을 택했나?" → SSAFY 공식 인증 API가 없어, 전 교육생이 보유한 계정 체계를 활용하는 것이 확실하고 구현 비용이 낮았다.
- "보안 우려는?" → 자격 증명은 검증 호출에만 사용하고 저장하지 않는 것이 원칙. (실제 구현이 어땠는지 본인 확인 필요 — 저장 안 했다면 그렇게 답하기)

### 3. Spring Security 필터 체인

**개념** — Spring Security는 서블릿 필터 체인으로 동작한다. 요청이 컨트롤러에 닿기 전에 여러 필터를 차례로 통과하며 인증·인가가 처리된다. JWT 방식에서는 보통 `UsernamePasswordAuthenticationFilter` 앞에 **커스텀 JWT 필터**를 끼워 넣는다.

**우리 구현** — JWT 필터가 헤더에서 토큰을 꺼내 검증하고, 유효하면 `SecurityContextHolder`에 `Authentication` 객체를 넣는다. 이후 컨트롤러에서 `@AuthenticationPrincipal` 등으로 사용자 정보를 꺼내 쓴다. 세션은 `STATELESS`로 설정.

**예상 질문**
- "필터에서 예외가 나면?" → 필터는 컨트롤러 밖이라 `@ControllerAdvice`가 못 잡는다. `AuthenticationEntryPoint`(401)/`AccessDeniedHandler`(403) 또는 별도 예외 처리 필터로 응답을 만든다.
- "인증(Authentication)과 인가(Authorization) 차이?" → 인증은 "누구인가", 인가는 "무엇을 할 수 있는가". 필터 체인에서 인증 후, URL/메서드 규칙으로 인가를 검사.

### 4. 커서 기반 페이지네이션

**개념** — `OFFSET n LIMIT m`은 페이지가 깊어질수록 n개를 읽고 버려서 느려지고, 사이에 글이 삽입되면 중복/누락이 생긴다. 커서 방식은 "마지막으로 본 항목의 키(예: id)"를 기준으로 `WHERE id < :cursor ORDER BY id DESC LIMIT m`으로 다음 페이지를 가져온다.

**우리 구현** — 게시판 무한 스크롤에 커서 방식 적용. 응답에 다음 요청용 커서(마지막 게시글 id)를 담아 보낸다.

**예상 질문**
- "왜 offset이 아니라 커서인가?" → 모바일 무한 스크롤 특성상 깊은 페이지 접근이 잦고, 실시간으로 글이 추가되는 커뮤니티라 offset의 중복 노출 문제가 실제 UX 문제가 된다. 커서는 인덱스를 타고 시작점으로 바로 점프하므로 일정한 성능.
- "단점은?" → 임의 페이지(예: 37페이지로 점프) 이동이 어렵다. 무한 스크롤 UI라 문제가 되지 않았다.

### 5. HOT 게시판 — Redis 3분 TTL 캐시

**개념** — 좋아요·조회수 집계로 "지금 뜨는 글"을 뽑는 쿼리는 무겁다. 결과가 초 단위로 정확할 필요가 없으므로, 계산 결과를 Redis에 TTL과 함께 저장하고 만료 전까지 재사용한다.

**우리 구현** — HOT 게시판 목록을 Redis에 3분 TTL로 캐시. 캐시 히트면 DB를 건드리지 않고, 미스면 집계 쿼리 후 다시 캐시(look-aside 패턴).

**예상 질문**
- "왜 3분인가?" → '실시간처럼 느껴지는 신선도'와 'DB 집계 부하'의 트레이드오프. HOT 순위가 3분 안에 급변하는 일은 드물어 체감 차이가 없다.
- "캐시 스탬피드(만료 순간 요청 몰림)는?" → 트래픽 규모상 문제가 없었지만, 커지면 락으로 1개 요청만 재계산하거나 TTL 지터, 백그라운드 갱신으로 완화할 수 있다.

### 6. 인기 검색어 — Redis Sorted Set

**개념** — Sorted Set(ZSET)은 멤버마다 score를 가진 정렬 집합. `ZINCRBY`로 검색어 점수를 올리고 `ZREVRANGE`로 상위 N개를 뽑으면 그대로 실시간 랭킹이 된다. 삽입/갱신이 O(log N).

**우리 구현** — 검색 발생 시 해당 키워드를 `ZINCRBY`, 인기 검색어 조회는 상위 N개 `ZREVRANGE`.

**예상 질문**
- "오래된 검색어가 계속 남지 않나?" → 시간 윈도우(예: 시간대별 키를 만들고 만료)나 주기적 리셋으로 최신성 유지. (실제 어떤 방식을 썼는지 본인 확인 필요)
- "왜 DB 집계가 아니라 Redis인가?" → 검색마다 DB write가 생기는 걸 피하고, 메모리 기반 O(log N) 연산으로 랭킹 조회가 즉시 끝난다.

### 7. 실시간 채팅 — WebSocket STOMP

**개념** — WebSocket은 한 번의 핸드셰이크로 양방향 연결을 유지하는 프로토콜. 다만 원시 WebSocket에는 "어느 방에 보낼지" 같은 규약이 없어서, 그 위에 **STOMP**(구독/발행 메시징 프로토콜)를 얹는다. 클라이언트는 `/topic/room.{id}`를 구독(subscribe)하고, 서버는 그 토픽으로 발행(publish)하면 구독자 전원에게 전달된다.

**우리 구현**
- Spring의 `@EnableWebSocketMessageBroker` + STOMP 엔드포인트, 방별 토픽 라우팅.
- **핸들러 레벨 JWT 검증**: WebSocket은 연결 후 HTTP 헤더 개념이 없어, STOMP CONNECT 프레임의 헤더로 JWT를 받아 `ChannelInterceptor`에서 검증했다.
- **Nginx WebSocket 프록시**: Nginx는 기본적으로 Upgrade 헤더를 전달하지 않으므로 `proxy_set_header Upgrade $http_upgrade; proxy_set_header Connection "upgrade";` 설정으로 WebSocket 통과를 열었다.
- SockJS 폴백으로 WebSocket 미지원 환경 대응.

**예상 질문**
- "왜 Polling이 아니라 WebSocket인가?" → 폴링은 새 메시지가 없어도 주기적으로 요청이 발생(서버 부하·지연). 채팅은 저지연 양방향이 본질이라 WebSocket이 맞다.
- "연결이 끊기면?" → 클라이언트 재연결 + 재구독. 놓친 메시지는 DB에 저장된 이력 조회로 복구.
- "서버를 늘리면(스케일아웃) 어떻게 되나?" → 인메모리 SimpleBroker는 서버 간 공유가 안 되므로, Redis Pub/Sub이나 RabbitMQ 같은 외부 브로커 릴레이가 필요해진다 — 확장 시 과제로 인지하고 있다고 답하면 좋다.

### 8. 팀/스터디 매칭 — 생명주기 API

**개념** — 팀이라는 도메인을 상태 머신으로 모델링: 생성 → 모집 중 → 운영 중 → 종료. 상태마다 가능한 행위(지원/수락/거절, Task 관리)가 다르다.

**우리 구현** — 상태 전이를 서버에서 검증(모집 마감된 팀에 지원 불가 등), 지원-수락-거절 흐름과 팀 내부 Task 관리 API 구현.

**예상 질문**
- "동시에 두 명이 마지막 자리에 수락되면?" → 정원 검증과 수락을 하나의 트랜잭션으로 묶고, 필요 시 락(비관적 락 또는 유니크 제약)으로 초과 수락 방지. (실제 처리 방식 본인 확인)

### 9. FCM 푸시 — Firebase Admin SDK + @Async

**개념** — FCM(Firebase Cloud Messaging)은 앱에 푸시를 보내는 구글 인프라. 서버는 Admin SDK로 기기 토큰에 메시지를 보낸다. 전송은 외부 네트워크 호출이라 느릴 수 있으므로, 본 요청 트랜잭션과 분리해 **@Async 비동기**로 보낸다.

**우리 구현** — 댓글·수락 등 이벤트 발생 시 대상 사용자의 FCM 토큰으로 푸시. 전송 로직을 `@Async` 메서드로 분리해 API 응답 지연을 제거.

**예상 질문**
- "@Async가 동작하는 원리는?" → 프록시 기반. 같은 클래스 내부 호출(self-invocation)은 프록시를 안 타서 비동기가 안 된다 — 별도 빈으로 분리해야 한다. 스레드풀은 `TaskExecutor` 설정.
- "전송 실패하면?" → 푸시는 best-effort. 실패 로깅 후 서비스 흐름에는 영향 없게 설계. 만료 토큰은 정리.

### 10. DB 설계 — ERD 반복, Soft Delete

**개념** — Soft Delete는 행을 지우지 않고 `deletedAt` 타임스탬프로 "지워진 셈" 처리하는 방식. FK 무결성이 유지되고 복구·감사가 가능하지만, 모든 조회에 `deletedAt IS NULL` 조건이 필요해진다.

**우리 구현**
- ERD를 1~3차 반복 설계(15+ 엔티티, 44 Repository). 기능 추가마다 관계를 재검토했고, 설계에 들인 시간만큼 구현 기간이 줄어드는 걸 체감.
- **댓글 Soft Delete(트리 보존)**: 대댓글이 달린 댓글을 물리 삭제하면 트리가 끊어진다. "삭제된 댓글입니다"로 표시만 바꿔 트리 구조를 보존.

**예상 질문**
- "JPA에서 Soft Delete는 어떻게?" → 엔티티에 `deletedAt` 필드, 삭제 시 update. `@SQLDelete`(delete를 update문으로 치환)와 `@Where(clause = "deleted_at is null")`로 자동화 가능.
- "N+1 문제를 아는가?" → 연관 엔티티를 지연 로딩으로 두고 컬렉션을 순회하면 건마다 쿼리가 나가는 문제. Fetch Join / `@EntityGraph` / batch size로 해결. (팀 매칭 도메인에서 LazyInitializationException을 수정한 경험과 연결해 답하기)

### 11. 트러블슈팅 3건

**11-1. Jackson 3 + Redis 직렬화 연쇄 장애**
- 상황: Spring Boot 4.0이 Jackson 3을 사용 → 패키지가 `com.fasterxml.jackson`에서 `tools.jackson`으로 바뀌면서 기존 Redis 직렬화 설정이 깨짐.
- 문제의 본질: Redis에 객체를 저장하려면 직렬화가 필요한데, 기본 JDK 직렬화는 클래스 버전에 취약하고 가독성이 없다. JSON 직렬화로 바꾸되, 역직렬화 시 어떤 클래스로 복원할지 **타입 정보**가 필요하다.
- 해결: `GenericJackson3JsonRedisSerializer` — JSON에 `@class` 필드로 타입 정보를 포함해 저장, 역직렬화 실패 제거.
- 교훈으로 답하기: "최신 메이저 버전을 도입할 때는 의존성 연쇄 변경을 사전 검증해야 한다. 다음엔 안정화된 LTS 기반으로 시작하겠다."

**11-2. 회원탈퇴 Side Effect — 4일의 추적**
- 상황: 탈퇴를 Hard Delete로 구현하자 회원을 참조하던 곳(게시글, 댓글, 팀 리더 등)에서 FK 제약 오류와 null 참조가 연쇄적으로 발생.
- 해결: ① 회원을 Soft Delete(`deletedAt`)로 전환 ② 팀 리더 탈퇴 시 리더 null 처리 + 팀원에게 경고 노출 ③ 게시물 작성자는 "탈퇴한 사용자"로 표시.
- 답 포인트: "삭제는 기술 문제이기 전에 정책 문제였다. 데이터 무결성(참조 보존)과 UX(탈퇴자 콘텐츠 표시), 개인정보(실제 파기 의무) 세 축을 함께 설계해야 함을 배웠다."

**11-3. API 스펙 반복 수정**
- 상황: Android 3명·iOS 1명, 클라이언트 파트와의 API 스펙이 3~5차례 반복 수정되며 개발이 지연.
- 해결: Request/Response body를 필드 단위로 상세 명세하고 Swagger(OpenAPI) 문서화를 체계화 → 이후 1차 리뷰 통과율 80% 이상.
- 답 포인트: "커뮤니케이션 비용도 설계 대상이다. 문서가 곧 인터페이스 계약임을 배웠다."

---

## PART 2 · 번역의 민족 (본인 담당 영역)

### 12. 3단계 패키징 파이프라인: PyInstaller → electron-builder → Inno Setup

**각 단계의 역할**
1. **PyInstaller** — Python으로 짠 FastAPI 백엔드를, Python 미설치 PC에서도 도는 **단일 실행 파일(exe)** 로 동결(freeze). 인터프리터와 의존성을 함께 묶는다.
2. **electron-builder** — Electron(크로미엄 기반 데스크톱 셸) 앱을 배포 가능한 형태로 빌드. 프론트엔드 UI와 1번에서 만든 백엔드 exe를 리소스로 포함.
3. **Inno Setup** — Windows 설치 마법사(setup.exe) 제작 도구. 설치 경로, 바로가기, 사전 조건 체크(디스크 용량 등), 언인스톨러까지 담당.

**왜 3단계인가** — 각 도구가 다른 층을 책임진다: Python 동결(런타임) → 앱 번들(제품) → 설치 경험(배포). 어느 하나로 대체 불가.

**예상 질문**
- "Electron 앱 안에서 Python 백엔드는 어떻게 뜨나?" → Electron 메인 프로세스가 앱 시작 시 백엔드 exe를 자식 프로세스로 실행하고, UI는 localhost로 FastAPI에 요청한다.
- "가장 어려웠던 점은?" → 17GB 모델을 포함한 설치 파일 자체 (아래 14번).

### 13. per-user 설치 (관리자 권한 없는 설치)

**개념** — `Program Files` 설치는 관리자 권한(UAC)이 필요하다. 강의실·공용 PC는 권한이 없는 경우가 많아, 사용자 프로필 영역(`%LOCALAPPDATA%`)에 설치하는 **per-user 방식**을 택하면 권한 없이 설치된다. Inno Setup에서는 `PrivilegesRequired=lowest` + 사용자 영역 기본 경로로 설정.

**답 포인트** — "타깃 사용자(강사)의 실행 환경 제약을 먼저 조사하고, '설치가 안 되면 제품이 아니다'는 기준으로 배포 방식을 정했다."

### 14. 번역의 민족 트러블슈팅 3건 (자소서 기준)

**14-1. 고사양 서버 미지원 → 온디바이스 피벗**
- 상황: 기업 요구 "2초 이내 실시간 번역". 명세서가 전제한 고사양 서버 지원이 무산.
- 결정: 3주간 팀과 검증 끝에 노트북 한 대에서 ASR→번역→TTS 전부 처리하는 온디바이스 앱으로 전환. 요구사항 명세 재작성·일정 재편은 팀장인 본인 담당.
- 답 포인트: "제약이 바뀌면 목표가 아니라 아키텍처를 바꾼다. 결정 근거(지연시간·비용·개인정보)를 명세로 남겨 팀 합의를 만들었다."

**14-2. 개발 환경에선 되던 AI 기능이 설치본에서 멈춤**
- 상황: 개발 PC에서 정상이던 AI 기능이 패키징된 설치본에서만 동작 안 함.
- 접근: 개발 환경과 설치 대상 PC의 차이(경로·번들 리소스·런타임)를 하나씩 대조하며 수정. 이후 "다른 PC에서 재현 확인"을 배포 절차에 넣음.
- `TODO`: 실제 원인이 무엇이었는지(예: 동결 경로, 누락 파일, DLL) 기억나면 구체화 — 면접에서 반드시 물어볼 항목.

**14-3. 관리자 권한 없는 강의실 PC 설치 실패**
- 해결: per-user 설치(관리자 권한 불필요) + 설치 전 디스크 용량 체크. 13번 참조.
- 답 포인트: "배포까지가 제품" — 비개발자가 설치 못 하면 기능은 없는 것.

### 15. FastAPI 기초 (일부 담당)

**개념** — Python 비동기 웹 프레임워크. 타입 힌트 기반으로 요청/응답 스키마(Pydantic)가 정의되고 OpenAPI 문서가 자동 생성된다. `async def` 엔드포인트로 I/O 병렬 처리.

**예상 질문**
- "Spring과 비교하면?" → FastAPI는 가볍고 빠르게 세우는 데 강점, Spring은 대규모 도메인 구조화(DI, AOP, 트랜잭션)에 강점. 이 프로젝트는 로컬 단일 프로세스 + AI 파이프라인이라 Python 생태계(모델 로딩)와 붙는 FastAPI가 자연스러웠다.

### 15-1. AWS EC2 배포·운영 (SSABREE)

**개념** — EC2는 AWS의 가상 서버. Spring Boot 앱을 EC2 인스턴스에서 구동하고, 앞단에 Nginx를 두어 리버스 프록시(HTTPS 종단, WebSocket Upgrade 통과)를 맡긴다.

**예상 질문**
- "배포 구성은?" → EC2 위에 Nginx → Spring Boot 구조. Nginx가 정적 응답/프록시/WebSocket Upgrade를 처리. (실제 HTTPS 인증서, DB 위치(RDS인지 EC2 내인지), 배포 절차는 본인 기억 확인 후 채울 것 — `TODO`)
- "서버가 죽으면 어떻게 알았나?" → 운영 기간(출시~2026.07) 동안의 모니터링/재시작 방식을 답할 수 있어야 함. (`TODO`: 실제 방식 확인)
- "지금은 왜 서비스를 내렸나?" → SSAFY 수료(2026.07)와 함께 교육생 전용 폐쇄형 서비스의 운영 주체·비용(EC2) 유지가 어려워 종료. "출시→운영→종료까지 서비스 생애주기를 한 번 완주해 봤다"는 프레임으로 답하기. 포트폴리오·이력서 어디에도 "현재 운영 중"이라 쓰지 않았는지 재확인.

**주의** — jQuery는 미경험이므로 면접에서 언급하지 말 것. 물어보면 "JS 기본기가 있어 jQuery는 필요 시 바로 습득 가능" 프레임으로.

### 16. 팀장 리딩 답변 프레임

- 갈등/조율 질문에는 구체 사례 1개를 준비: AI·Frontend·Backend가 만나는 **통합 지점(배포·일정·인터페이스)** 에서 내가 조율한 결정 하나를 STAR(상황-과제-행동-결과)로.
- `TODO`: 실제 조율 사례 1건을 기억나는 대로 채울 것.

---

## 부록 · 스스로 점검 체크리스트

각 항목을 보지 않고 1분간 소리 내어 설명할 수 있으면 통과:

- [ ] Access/Refresh를 나누는 이유와 만료 시 재발급 흐름
- [ ] Security 필터 체인에서 JWT가 검증되는 위치
- [ ] 커서 페이지네이션이 offset보다 나은 이유 2가지
- [ ] HOT 게시판 캐시의 look-aside 흐름과 TTL 트레이드오프
- [ ] ZINCRBY / ZREVRANGE로 랭킹이 만들어지는 과정
- [ ] STOMP의 구독/발행 모델과 CONNECT 프레임 JWT 검증 이유
- [ ] Soft Delete를 택한 이유와 JPA 구현 방법
- [ ] Jackson3 장애의 원인(패키지 변경)과 @class 타입 보존
- [ ] PyInstaller / electron-builder / Inno Setup 각각의 역할 한 줄
- [ ] per-user 설치가 필요한 이유
